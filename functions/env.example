# Development Environment Configuration
# Copy this file to .env and modify as needed

# Enable development utilities (wipe, seed, reset database)
# Set to "true" to enable dev utilities, "false" or unset to disable
ENABLE_DEV_UTILITIES=true

# Node environment
# Set to "development", "test", or "production" to control which database to use
NODE_ENV=development

# Firebase configuration
# Development database (current: kitty-680c6)
FIREBASE_PROJECT_ID_DEV=kitty-680c6

# Production database (set when you create it)
# FIREBASE_PROJECT_ID_PROD=kitty-prod

# Test environment uses emulators (no project ID needed) 